#ifndef __BLOOM_H__
#define __BLOOM_H__

#include<stdlib.h>
#include<stdio.h>
#include<string.h>

typedef struct {
unsigned int size;
char* strpool;
/*other fields*/
}BF;

BF *bloom_create();
int bloom_destroy();
int bloom_add();
int bloom_check();

#endif
